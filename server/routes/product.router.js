// Dependencies
const express = require("express");
const router = express.Router();
const productController = require('../controllers/product.controller')

// Routes
// router.route('/:Category/:query').get(productController.);

// Exports
module.exports = router;