import React from 'react';
import { Box, Typography } from '@mui/material';

const UserDetails = () => {
  return (
    <Box sx={{ flex: 1 }}>
      <Typography variant="h5">User Details section</Typography>
    </Box>
  );
};

export default UserDetails;
