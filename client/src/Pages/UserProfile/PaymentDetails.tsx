import React from 'react';
import { Box, Typography } from '@mui/material';

const PaymentDetails = () => {
  return (
    <Box sx={{ flex: 1 }}>
      <Typography variant="h5">Payment Details section</Typography>
    </Box>
  );
};

export default PaymentDetails;
